interface DateObject {
    day: number | string;
    month: number | string;
    year: number | string;
}
interface Options {
    zeroBasedMonth?: boolean;
    monthBeforeDay?: boolean;
}
/**
 * Check if a date is valid. Returns true or false.
 */
export declare const isDateValid: (date: string | DateObject, options?: Options) => boolean;
export {};
//# sourceMappingURL=isDateValid.d.ts.map