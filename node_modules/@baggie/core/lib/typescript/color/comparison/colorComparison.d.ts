/**
 * Find the nearest or farthest matching colors.
 */
export declare class ColorComparison {
    private haystack;
    /**
     * Create a set of colors to search for the closest match in.
     */
    constructor(colors: string[]);
    private static parseColorToLab;
    reset(): this;
    add(colors: string | string[]): this;
    compare(color: string): string[];
}
//# sourceMappingURL=colorComparison.d.ts.map