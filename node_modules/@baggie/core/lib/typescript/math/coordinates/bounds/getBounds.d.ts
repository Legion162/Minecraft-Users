import { Bounds } from "../_interfaces/bounds.interface";
import { CoordinatesXY } from "../_interfaces/coordinatesXY.interface";
/**
 * Get the boundaries from an array of coordinates.
 *
 * @example
 * **Basic usage:**
 * ```ts
 * import { getBounds } from "@baggie/core";
 *
 * const coordinates = [
 *     { x: 10, y: 5 },
 *     { x: 0, y: 10 },
 *     { x: -10, y: -10 },
 * ];
 *
 * const bounds = getBounds(coordinates);
 * // bounds = { min: { x: -10, y: -10 }, max: { x: 10, y: 10 }}
 * ```
 */
export declare const getBounds: (coordinates: CoordinatesXY[]) => Bounds;
//# sourceMappingURL=getBounds.d.ts.map