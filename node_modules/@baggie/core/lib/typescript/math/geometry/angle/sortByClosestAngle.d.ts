/**
 * Get an array of angles sorted by which is closest to the target.
 *
 * @example
 * **Basic usage:**
 * ```ts
 * import { sortByClosestAngle } from "@baggie/core";
 *
 * const target = 350;
 * const angles = [
 *     0,
 *     10,
 *     100,
 *     200,
 *     300,
 * ];
 * const sorted = sortByClosestAngle(target, angles);
 * // sorted = [0, 10, 300, 100, 200]
 * ```
 */
export declare const sortByClosestAngle: (target: number, angles: number[]) => number[];
//# sourceMappingURL=sortByClosestAngle.d.ts.map