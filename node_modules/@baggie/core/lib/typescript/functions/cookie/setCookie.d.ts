/**
 * These are the properties that can be set in the `attributes` objects of the
 * cookie functions.
 *
 * @remarks
 * Using `maxAge` is preferred over `expires`. If both `expires` and `maxAge`
 * are set, `maxAge` has precedence.
 */
export interface CookieAttributes {
    path?: string;
    domain?: string;
    maxAge?: number;
    expires?: number | Date;
    httpOnly?: boolean;
    secure?: boolean;
}
/**
 * Set a cookie with a name, value and optional attributes.
 */
export declare const setCookie: (name: string, value: any, attributes?: CookieAttributes) => void;
//# sourceMappingURL=setCookie.d.ts.map