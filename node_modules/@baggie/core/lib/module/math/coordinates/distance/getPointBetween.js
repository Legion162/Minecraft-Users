/**
 * Get a point between two sets of coordinates.
 *
 * @example
 * **Basic usage:**
 * ```ts
 * import { getPointBetween } from "@baggie/core";
 *
 * const from = { x: 10, y: 10 };
 * const to = { x: 20, y: 20 };
 * const progress = .5;
 *
 * const point = getPointBetween(from, to, progress);
 * // point = { x: 15, y: 15 }
 * ```
 */
export const getPointBetween = function (from, to) {
  let progress = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : 0.5;
  return {
    x: from.x + (to.x - from.x) * progress,
    y: from.y + (to.y - from.y) * progress
  };
};
//# sourceMappingURL=getPointBetween.js.map