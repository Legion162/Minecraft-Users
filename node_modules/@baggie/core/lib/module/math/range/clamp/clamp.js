/**
 * Make sure a number is within the range of a minimum and maximum value.
 */
export const clamp = (input, limits) => Math.min(limits.max ?? Infinity, Math.max(limits.min ?? -Infinity, input));
//# sourceMappingURL=clamp.js.map