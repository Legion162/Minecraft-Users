export const normalizeRanges = ranges => (!Array.isArray(ranges) || typeof ranges[0] === "number" ? [ranges] : ranges).map(interval => Array.isArray(interval) ? {
  from: interval[0],
  to: interval[1]
} : interval);
//# sourceMappingURL=normalizeRanges.js.map