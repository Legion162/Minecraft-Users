import { normalizeRanges } from "../_helpers/normalizeRanges";

/**
 * Check if a number is within the range of a minimum and maximum value.
 */
export const isInRange = (number, ranges) => normalizeRanges(ranges).reduce((inRange, _ref) => {
  let {
    from,
    to
  } = _ref;
  return inRange || number >= from && number <= to;
}, false);
//# sourceMappingURL=isInRange.js.map