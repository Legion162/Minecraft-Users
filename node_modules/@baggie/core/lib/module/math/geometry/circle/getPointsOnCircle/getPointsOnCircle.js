import { degreesToRadians } from "../../../radians/degreesToRadians";
import { getPercentage } from "../../../percentage/getPercentage/getPercentage";
/**
 * Get an array of equally distanced points on the perimeter of a circle.
 */
export const getPointsOnCircle = _ref => {
  let {
    radius,
    numberOfPoints,
    offset = 0
  } = _ref;
  const result = [];
  const degreesToFirstPoint = getPercentage({
    part: 1,
    whole: numberOfPoints
  }) * 360;
  for (let i = 1; i <= numberOfPoints; i += 1) {
    let degrees = getPercentage({
      part: i,
      whole: numberOfPoints
    }) * 360 - degreesToFirstPoint + offset % 360;
    const radians = degreesToRadians(degrees - 90);
    degrees = degrees < 0 ? 360 + degrees : degrees;
    result.push({
      degrees,
      x: radius * Math.cos(radians),
      y: radius * Math.sin(radians)
    });
  }
  return result;
};
//# sourceMappingURL=getPointsOnCircle.js.map