import { decimalAdjust } from "../_helpers/decimalAdjust.helper";

/**
 * Round a number to the nearest decimal places.
 */
export const roundDecimals = (number, maxDecimalPlaces) => decimalAdjust(number, maxDecimalPlaces, Math.round);
//# sourceMappingURL=roundDecimals.js.map