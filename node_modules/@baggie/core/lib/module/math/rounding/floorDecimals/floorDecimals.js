import { decimalAdjust } from "../_helpers/decimalAdjust.helper";

/**
 * Round down a number to the nearest decimal places.
 */
export const floorDecimals = (number, maxDecimalPlaces) => decimalAdjust(number, maxDecimalPlaces, Math.floor);
//# sourceMappingURL=floorDecimals.js.map