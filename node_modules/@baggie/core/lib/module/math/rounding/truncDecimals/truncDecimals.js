import { decimalAdjust } from "../_helpers/decimalAdjust.helper";

/**
 * Truncate a number to the nearest decimal places.
 */
export const truncDecimals = (number, maxDecimalPlaces) => decimalAdjust(number, maxDecimalPlaces, Math.trunc);
//# sourceMappingURL=truncDecimals.js.map