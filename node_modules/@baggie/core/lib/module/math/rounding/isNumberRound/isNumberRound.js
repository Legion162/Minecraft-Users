/**
 * Check if a number is rounded to the maximum desired amount of decimal places.
 */
export const isNumberRound = function (number) {
  let maxDecimalPlaces = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : 0;
  const p = Math.pow(10, maxDecimalPlaces);
  return Math.round(number * p) / p === number;
};
//# sourceMappingURL=isNumberRound.js.map