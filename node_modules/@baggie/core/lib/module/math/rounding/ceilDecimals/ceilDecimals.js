import { decimalAdjust } from "../_helpers/decimalAdjust.helper";

/**
 * Round up a number to the nearest decimal places.
 */
export const ceilDecimals = (number, maxDecimalPlaces) => decimalAdjust(number, maxDecimalPlaces, Math.ceil);
//# sourceMappingURL=ceilDecimals.js.map