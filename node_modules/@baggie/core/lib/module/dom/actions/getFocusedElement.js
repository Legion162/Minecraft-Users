import { isBrowser } from "../../detection";

/**
 * If an element is focused, return it.
 */
export const getFocusedElement = () => {
  var _document;
  return isBrowser && ((_document = document) === null || _document === void 0 ? void 0 : _document.activeElement) || null;
};
//# sourceMappingURL=getFocusedElement.js.map