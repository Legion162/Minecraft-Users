import { isBrowser } from "../../environment/isBrowser";

/**
 * Match media
 *
 * @internal
 * @param string - The media string to test
 * @returns `true` if the media string matches an applied feature
 */
export const dontMatchMedia = string => isBrowser && window.matchMedia && !window.matchMedia(string).matches;
//# sourceMappingURL=dontMatchMedia.helper.js.map