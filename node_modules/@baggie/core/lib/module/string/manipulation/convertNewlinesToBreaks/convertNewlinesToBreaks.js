import { getNewlineRegex } from "../../regex/getNewlineRegex/getNewlineRegex";

/**
 * Replace newlines with break elements.
 */
export const convertNewlinesToBreaks = function (string) {
  let keepWhitespace = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : false;
  return string.replace(getNewlineRegex(!keepWhitespace), "<br />");
};
//# sourceMappingURL=convertNewlinesToBreaks.js.map