/**
 * Get part of a string between the start and end indexes, or to the end of the
 * string. HTML entities are each counted as one character.
 */
export const substringHtmlEntitySafe = (string, indexStart, indexEnd, options) => {
  const end = typeof indexEnd === "undefined" ? undefined : indexEnd < 1 ? indexEnd - 1 : indexEnd;
  const splitString = string.match(/&#?[a-z0-9]+;|.?/gi);
  const output = splitString.slice(indexStart, end).join("");
  const realIndexStart = options !== null && options !== void 0 && options.verbose ? splitString.slice(0, indexStart).join("").length : 0;
  return options !== null && options !== void 0 && options.verbose ? {
    output,
    realIndexStart,
    realIndexEnd: realIndexStart + output.length
  } : output;
};
//# sourceMappingURL=substringHtmlEntitySafe.js.map