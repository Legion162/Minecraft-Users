import { getTagsRegex } from "../../regex/getTagsRegex/getTagsRegex";

/**
 * Strip HTML and XML tags from a string. Tags are replaced with a space unless .
 */
export const stripTagsFromString = function (string) {
  let replaceWith = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : " ";
  let trim = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : true;
  const newString = string.replace(getTagsRegex(true), replaceWith);
  return trim ? newString.trim() : newString;
};
//# sourceMappingURL=stripTagsFromString.js.map