import { getPasswordRegex } from "../../regex/getPasswordRegex/getPasswordRegex";
/**
 * Check if a password is valid.
 */
export const isPasswordValid = (password, options) => getPasswordRegex(options).test(typeof password === "string" ? password : password.value);
//# sourceMappingURL=isPasswordValid.js.map