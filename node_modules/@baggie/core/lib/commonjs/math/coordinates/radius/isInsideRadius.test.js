"use strict";

var _isInsideRadius = require("./isInsideRadius");
describe("isInsideRadius", () => {
  test("Inside radius", () => {
    expect((0, _isInsideRadius.isInsideRadius)({
      x: 20,
      y: 30
    }, 100)).toBe(true);
  });
  test("Outside radius", () => {
    expect((0, _isInsideRadius.isInsideRadius)({
      x: 20,
      y: 30
    }, 35)).toBe(false);
  });
});
//# sourceMappingURL=isInsideRadius.test.js.map