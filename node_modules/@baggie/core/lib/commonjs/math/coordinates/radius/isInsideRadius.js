"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.isInsideRadius = void 0;
/**
 * Check if a set of coordinates is inside a radius from the origin (0,0)
 *
 * @example
 * **Basic usage:**
 * ```ts
 * import { isInsideRadius } from "@baggie/core";
 *
 * const position = { x: 20, y: 30 };
 *
 * const radiusA = 100;
 * const checkA = isInsideRadius(position, radiusA);
 * // checkA = true
 *
 * const radiusB = 35;
 * const checkB = isInsideRadius(position, radiusB);
 * // checkB = false
 * ```
 */
const isInsideRadius = (position, radius) => Math.sqrt(position.x * position.x + position.y * position.y) < radius;
exports.isInsideRadius = isInsideRadius;
//# sourceMappingURL=isInsideRadius.js.map