"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.convertToCoordinates = void 0;
var _zeroIfNotNumber = require("../helpers/zeroIfNotNumber/zeroIfNotNumber");
/**
 * Convert a single number or an array with two numbers to a set of coordinates.
 *
 * @example
 * **Basic usage:**
 * ```ts
 * import { convertToCoordinates } from "@baggie/core";
 *
 * const number = convertToCoordinates(10);
 * // number = { x: 10, y: 10 }
 *
 * const array = convertToCoordinates([10, 20]);
 * // array = { x: 10, y: 20 }
 *
 * const object = convertToCoordinates({ x: 10, y: 20 });
 * // object = { x: 10, y: 20 }
 *
 * const nothing = convertToCoordinates();
 * // nothing = { x: 0, y: 0 }
 * ```
 */
const convertToCoordinates = coordinates => typeof coordinates === "object" && Object.prototype.hasOwnProperty.call(coordinates, "x") && Object.prototype.hasOwnProperty.call(coordinates, "y") ? {
  x: (0, _zeroIfNotNumber.zeroIfNotNumber)(coordinates.x),
  y: (0, _zeroIfNotNumber.zeroIfNotNumber)(coordinates.y)
} : Array.isArray(coordinates) ? {
  x: (0, _zeroIfNotNumber.zeroIfNotNumber)(coordinates[0]),
  y: (0, _zeroIfNotNumber.zeroIfNotNumber)(coordinates[1])
} : {
  x: (0, _zeroIfNotNumber.zeroIfNotNumber)(coordinates),
  y: (0, _zeroIfNotNumber.zeroIfNotNumber)(coordinates)
};
exports.convertToCoordinates = convertToCoordinates;
//# sourceMappingURL=convertToCoordinates.js.map