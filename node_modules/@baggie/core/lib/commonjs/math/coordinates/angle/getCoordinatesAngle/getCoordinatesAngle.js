"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getCoordinatesAngle = void 0;
var _radiansToDegrees = require("../../../radians/radiansToDegrees");
/**
 * Get the angle from one set of coordinates to another.
 */
const getCoordinatesAngle = (from, to, options) => {
  if (from.x === to.x && from.y === to.y) return 0;
  const settings = {
    clockwise: false,
    offset: 0,
    ...options
  };
  const x = to.x - from.x;
  const y = settings.clockwise ? to.y - from.y : from.y - to.y;
  const theta = Math.atan2(y, x);
  const angle = (0, _radiansToDegrees.radiansToDegrees)(theta) + 180 % 360 + settings.offset;
  return settings.offset === 0 ? angle : angle >= 360 ? angle - 360 : angle < 0 ? angle + 360 : angle;
};
exports.getCoordinatesAngle = getCoordinatesAngle;
//# sourceMappingURL=getCoordinatesAngle.js.map