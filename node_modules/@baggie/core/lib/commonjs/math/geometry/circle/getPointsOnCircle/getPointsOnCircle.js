"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getPointsOnCircle = void 0;
var _degreesToRadians = require("../../../radians/degreesToRadians");
var _getPercentage = require("../../../percentage/getPercentage/getPercentage");
/**
 * Get an array of equally distanced points on the perimeter of a circle.
 */
const getPointsOnCircle = _ref => {
  let {
    radius,
    numberOfPoints,
    offset = 0
  } = _ref;
  const result = [];
  const degreesToFirstPoint = (0, _getPercentage.getPercentage)({
    part: 1,
    whole: numberOfPoints
  }) * 360;
  for (let i = 1; i <= numberOfPoints; i += 1) {
    let degrees = (0, _getPercentage.getPercentage)({
      part: i,
      whole: numberOfPoints
    }) * 360 - degreesToFirstPoint + offset % 360;
    const radians = (0, _degreesToRadians.degreesToRadians)(degrees - 90);
    degrees = degrees < 0 ? 360 + degrees : degrees;
    result.push({
      degrees,
      x: radius * Math.cos(radians),
      y: radius * Math.sin(radians)
    });
  }
  return result;
};
exports.getPointsOnCircle = getPointsOnCircle;
//# sourceMappingURL=getPointsOnCircle.js.map