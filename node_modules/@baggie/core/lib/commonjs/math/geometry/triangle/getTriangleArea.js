"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getTriangleArea = void 0;
var _getTrianglePerimeter = require("./getTrianglePerimeter");
/**
 * Get the surface area of a triangle.
 *
 * @example
 * **Basic usage:**
 * ```ts
 * import { getTriangleArea } from "@baggie/core";
 *
 * const a = 10;
 * const b = 15;
 * const c = 20;
 * const surfaceArea = getTriangleArea(a, b, c);
 * // surfaceArea = 205.3959590644373
 * ```
 */
const getTriangleArea = (a, b, c) => {
  const semiPerimeter = (0, _getTrianglePerimeter.getTrianglePerimeter)(a, b, c) / 2;
  return Math.sqrt(semiPerimeter * (-a + b + c) * (a - b + c) * (a + b - c));
};
exports.getTriangleArea = getTriangleArea;
//# sourceMappingURL=getTriangleArea.js.map