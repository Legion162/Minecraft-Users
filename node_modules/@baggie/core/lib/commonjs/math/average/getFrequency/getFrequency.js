"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getFrequency = void 0;
/**
 * Get the frequency of each item in an array of numbers or strings.
 */
const getFrequency = (values, returnMap) => {
  const map = new Map();
  values.forEach(value => map.set(value, map.has(value) ? (map.get(value) || 0) + 1 : 1));
  return returnMap ? map : Array.from(map, _ref => {
    let [value, frequency] = _ref;
    return {
      value,
      frequency
    };
  }).sort((a, b) => a.frequency < b.frequency ? -1 : a.frequency > b.frequency ? 1 : 0);
};
exports.getFrequency = getFrequency;
//# sourceMappingURL=getFrequency.js.map