"use strict";

var _getAntiMode = require("./getAntiMode");
describe("getAntiMode", () => {
  test("Get a single anti mode value of an array", () => {
    expect((0, _getAntiMode.getAntiMode)([4, 8, 8, 15, 15, 16, 16, 42, 42, 42])).toMatchObject({
      numbers: [4],
      frequency: 1
    });
  });
  test("Get a single anti mode value of an array (larger frequency)", () => {
    expect((0, _getAntiMode.getAntiMode)([3, 3, 3, 3, 4, 4, 8, 8, 8, 16, 16, 16, 42, 42, 42])).toMatchObject({
      numbers: [4],
      frequency: 2
    });
  });
  test("Get multiple anti mode values of an array", () => {
    expect((0, _getAntiMode.getAntiMode)([4, 8, 8, 8, 15, 16, 23, 42, 42, 42])).toMatchObject({
      numbers: [4, 15, 16, 23],
      frequency: 1
    });
  });
  test("Get the anti mode value of an array of 1 number", () => {
    expect((0, _getAntiMode.getAntiMode)([4])).toMatchObject({
      numbers: [4],
      frequency: 1
    });
  });
  test("Get the anti mode value of an empty array", () => {
    expect((0, _getAntiMode.getAntiMode)([])).toMatchObject({
      numbers: [],
      frequency: 0
    });
  });
});
//# sourceMappingURL=getAntiMode.test.js.map