"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getPercentage = void 0;
/**
 * Perform calculations with percentage.
 */
const getPercentage = function (_ref) {
  let {
    part,
    whole,
    percentage
  } = _ref;
  let percentageAsDecimal = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : true;
  const factor = percentageAsDecimal ? 1 : 100;
  if (part && whole) {
    return part / whole * factor;
  }
  if (whole && percentage) {
    return percentage / factor * whole;
  }
  if (part && percentage) {
    return part / (percentage / factor);
  }
  return 0;
};
exports.getPercentage = getPercentage;
//# sourceMappingURL=getPercentage.js.map