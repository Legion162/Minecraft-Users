"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getTimeDifference = void 0;
/**
 * Calculate the difference between two timestamps.
 */
const getTimeDifference = (fromTimestampMs, toTimestampMs) => {
  let difference = Math.abs(fromTimestampMs - toTimestampMs);
  const onlyMilliseconds = difference;
  difference = Math.floor(difference / 1000);
  const milliseconds = onlyMilliseconds - difference * 1000;
  const seconds = difference % 60;
  difference = Math.floor(difference / 60);
  const minutes = difference % 60;
  difference = Math.floor(difference / 60);
  const hours = difference % 24;
  difference = Math.floor(difference / 24);
  const days = difference;
  return {
    days,
    hours,
    minutes,
    seconds,
    milliseconds
  };
};
exports.getTimeDifference = getTimeDifference;
//# sourceMappingURL=getTimeDifference.js.map