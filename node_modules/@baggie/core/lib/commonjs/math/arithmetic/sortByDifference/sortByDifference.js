"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.sortByDifference = void 0;
/**
 * Sort an array of numbers by their distance to a target number.
 */
const sortByDifference = (target, array, getDifference) => {
  const getSortingValue = typeof getDifference === "function" ? getDifference : input => input;
  const realTarget = typeof target === "number" ? target : getSortingValue(target);
  return [...array].sort((a, b) => Math.abs(realTarget - getSortingValue(a)) - Math.abs(realTarget - getSortingValue(b)));
};
exports.sortByDifference = sortByDifference;
//# sourceMappingURL=sortByDifference.js.map