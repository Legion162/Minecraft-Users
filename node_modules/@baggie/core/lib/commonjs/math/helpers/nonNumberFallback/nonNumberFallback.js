"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.nonNumberFallback = void 0;
/**
 * Return fallback if input is not and can't be converted into a number.
 */
const nonNumberFallback = (value, fallback) => {
  if (typeof value === "number") return value;
  if (typeof value === "string") {
    const parsed = parseFloat(value);
    if (!isNaN(parsed)) return parsed;
  }
  return fallback;
};
exports.nonNumberFallback = nonNumberFallback;
//# sourceMappingURL=nonNumberFallback.js.map