"use strict";

var _getColorDarkness = require("./getColorDarkness");
describe("getColorDarkness", () => {
  test("White", () => {
    expect((0, _getColorDarkness.getColorDarkness)({
      red: 255,
      green: 255,
      blue: 255
    })).toBe(0);
  });
  test("Black", () => {
    expect((0, _getColorDarkness.getColorDarkness)({
      red: 0,
      green: 0,
      blue: 0
    })).toBe(1);
  });
  test("Grey", () => {
    expect((0, _getColorDarkness.getColorDarkness)({
      red: 127,
      green: 127,
      blue: 127
    })).toBe(0.5019607843137255);
  });
});
//# sourceMappingURL=getColorDarkness.test.js.map