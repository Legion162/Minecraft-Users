"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getColorBrightness = void 0;
var _convertStringToRgb = require("../../convert/convertStringToRgb/convertStringToRgb");
/**
 * Get the perceived brightness of a color as percentage in decimal form.
 */
const getColorBrightness = color => {
  try {
    const {
      red,
      green,
      blue
    } = typeof color === "string" ? (0, _convertStringToRgb.convertStringToRgb)(color) : color;
    const perceivedBrightness = Math.sqrt(0.299 * (red * red) + 0.587 * (green * green) + 0.114 * (blue * blue));

    // Return perceived brightness as percentage in decimal form
    return perceivedBrightness / 2.55 / 100;
  } catch (err) {
    return 0;
  }
};
exports.getColorBrightness = getColorBrightness;
//# sourceMappingURL=getColorBrightness.js.map