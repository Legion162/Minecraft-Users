"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.multiplyColors = void 0;
var _math = require("../../../math");
var _convertStringToRgb = require("../../convert/convertStringToRgb/convertStringToRgb");
var _convertRgbToHex = require("../../convert/convertRgbToHex/convertRgbToHex");
/**
 * Blend two colors together with the multiply mode.
 */
const multiplyColors = (colorA, colorB) => {
  try {
    const rgb1 = typeof colorA === "string" ? (0, _convertStringToRgb.convertStringToRgb)(colorA) : colorA;
    const rgb2 = typeof colorB === "string" ? (0, _convertStringToRgb.convertStringToRgb)(colorB) : colorB;
    return (0, _convertRgbToHex.convertRgbToHex)({
      red: Math.round(rgb1.red * rgb2.red / 255),
      green: Math.round(rgb1.green * rgb2.green / 255),
      blue: Math.round(rgb1.blue * rgb2.blue / 255),
      alpha: Math.min((0, _math.getMean)([rgb1.alpha || 1, rgb2.alpha || 1]), 1)
    });
  } catch (err) {
    return undefined;
  }
};
exports.multiplyColors = multiplyColors;
//# sourceMappingURL=multiplyColors.js.map