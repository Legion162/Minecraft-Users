"use strict";

var _convertStringToRgb = require("./convertStringToRgb");
describe("convertStringToRgb", () => {
  test("Red", () => {
    expect((0, _convertStringToRgb.convertStringToRgb)("#ff0000")).toEqual(expect.objectContaining({
      alpha: 1,
      blue: 0,
      green: 0,
      red: 255
    }));
  });
  test("Lime", () => {
    expect((0, _convertStringToRgb.convertStringToRgb)("#00ff00")).toEqual(expect.objectContaining({
      alpha: 1,
      blue: 0,
      green: 255,
      red: 0
    }));
  });
  test("Blue", () => {
    expect((0, _convertStringToRgb.convertStringToRgb)("#0000ff")).toEqual(expect.objectContaining({
      alpha: 1,
      blue: 255,
      green: 0,
      red: 0
    }));
  });
  test("Alpha", () => {
    expect((0, _convertStringToRgb.convertStringToRgb)("#0000ff00")).toEqual(expect.objectContaining({
      alpha: 0,
      blue: 255,
      green: 0,
      red: 0
    }));
  });
});
//# sourceMappingURL=convertStringToRgb.test.js.map