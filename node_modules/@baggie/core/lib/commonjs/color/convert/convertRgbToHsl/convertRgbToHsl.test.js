"use strict";

var _convertRgbToHsl = require("./convertRgbToHsl");
describe("convertHslToRgb", () => {
  test("Red", () => {
    expect((0, _convertRgbToHsl.convertRgbToHsl)({
      red: 255,
      green: 0,
      blue: 0
    })).toEqual(expect.objectContaining({
      hue: 0,
      saturation: 1,
      lightness: 0.5,
      alpha: 1
    }));
  });
  test("Lime", () => {
    expect((0, _convertRgbToHsl.convertRgbToHsl)({
      red: 0,
      green: 255,
      blue: 0
    })).toEqual(expect.objectContaining({
      hue: 120,
      saturation: 1,
      lightness: 0.5,
      alpha: 1
    }));
  });
  test("Blue", () => {
    expect((0, _convertRgbToHsl.convertRgbToHsl)({
      red: 0,
      green: 0,
      blue: 255
    })).toEqual(expect.objectContaining({
      hue: 240,
      saturation: 1,
      lightness: 0.5,
      alpha: 1
    }));
  });
  test("Alpha", () => {
    expect((0, _convertRgbToHsl.convertRgbToHsl)({
      red: 0,
      green: 0,
      blue: 255,
      alpha: 0.5
    })).toEqual(expect.objectContaining({
      hue: 240,
      saturation: 1,
      lightness: 0.5,
      alpha: 0.5
    }));
  });
});
//# sourceMappingURL=convertRgbToHsl.test.js.map