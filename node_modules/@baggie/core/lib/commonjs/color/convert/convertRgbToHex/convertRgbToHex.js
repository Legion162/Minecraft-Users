"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.convertRgbToHex = void 0;
/**
 * Convert an object with RGB color values to a hex color.
 */
const convertRgbToHex = _ref => {
  let {
    red,
    green,
    blue,
    alpha
  } = _ref;
  const hasAlpha = typeof alpha === "number";
  const alphaValue = hasAlpha ? alpha : 1;
  const hexColor = [`0${red.toString(16)}`.slice(-2), `0${green.toString(16)}`.slice(-2), `0${blue.toString(16)}`.slice(-2), hasAlpha && alpha < 1 ? `0${Math.round(alphaValue * 255).toString(16)}`.slice(-2) : ""];
  return `#${hexColor.join("")}`;
};
exports.convertRgbToHex = convertRgbToHex;
//# sourceMappingURL=convertRgbToHex.js.map