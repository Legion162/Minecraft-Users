"use strict";

var _normalizeHexColor = require("./normalizeHexColor");
describe("normalizeHexColor", () => {
  test("Short hex uppercase", () => {
    expect((0, _normalizeHexColor.normalizeHexColor)("#FFF")).toEqual("#ffffff");
  });
  test("Short hex with alpha", () => {
    expect((0, _normalizeHexColor.normalizeHexColor)("#fC07")).toEqual("#ffcc0077");
  });
  test("Long hex", () => {
    expect((0, _normalizeHexColor.normalizeHexColor)("#FFFFFF")).toEqual("#ffffff");
  });
  test("Long hex with alpha", () => {
    expect((0, _normalizeHexColor.normalizeHexColor)("#FFccFF75")).toEqual("#ffccff75");
  });
  test("Not hex", () => {
    expect((0, _normalizeHexColor.normalizeHexColor)("lorem")).toEqual("lorem");
  });
});
//# sourceMappingURL=normalizeHexColor.test.js.map