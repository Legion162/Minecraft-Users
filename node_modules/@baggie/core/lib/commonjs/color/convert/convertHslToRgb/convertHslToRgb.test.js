"use strict";

var _convertHslToRgb = require("./convertHslToRgb");
describe("convertHslToRgb", () => {
  test("Red", () => {
    expect((0, _convertHslToRgb.convertHslToRgb)({
      hue: 0,
      saturation: 1,
      lightness: 0.5
    })).toEqual(expect.objectContaining({
      red: 255,
      green: 0,
      blue: 0,
      alpha: 1
    }));
  });
  test("Lime", () => {
    expect((0, _convertHslToRgb.convertHslToRgb)({
      hue: 120,
      saturation: 1,
      lightness: 0.5
    })).toEqual(expect.objectContaining({
      red: 0,
      green: 255,
      blue: 0,
      alpha: 1
    }));
  });
  test("Blue", () => {
    expect((0, _convertHslToRgb.convertHslToRgb)({
      hue: 240,
      saturation: 1,
      lightness: 0.5
    })).toEqual(expect.objectContaining({
      red: 0,
      green: 0,
      blue: 255,
      alpha: 1
    }));
  });
  test("Alpha", () => {
    expect((0, _convertHslToRgb.convertHslToRgb)({
      hue: 240,
      saturation: 1,
      lightness: 0.5,
      alpha: 0.5
    })).toEqual(expect.objectContaining({
      red: 0,
      green: 0,
      blue: 255,
      alpha: 0.5
    }));
  });
});
//# sourceMappingURL=convertHslToRgb.test.js.map