"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getColorSchemeTetradic = void 0;
var _convertHslToRgb = require("../../convert/convertHslToRgb/convertHslToRgb");
var _convertRgbToHsl = require("../../convert/convertRgbToHsl/convertRgbToHsl");
var _convertRgbToHex = require("../../convert/convertRgbToHex/convertRgbToHex");
var _convertStringToRgb = require("../../convert/convertStringToRgb/convertStringToRgb");
/**
 * Get a tetradic color scheme.
 */
const getColorSchemeTetradic = color => {
  const hex = typeof color === "string" ? color : (0, _convertRgbToHex.convertRgbToHex)(color);
  const rgb = typeof color === "string" ? (0, _convertStringToRgb.convertStringToRgb)(color) : color;
  const {
    hue,
    saturation,
    lightness,
    alpha
  } = (0, _convertRgbToHsl.convertRgbToHsl)(rgb);
  return [hex, (0, _convertRgbToHex.convertRgbToHex)((0, _convertHslToRgb.convertHslToRgb)({
    hue: (hue + 90) % 360,
    saturation,
    lightness,
    alpha
  })), (0, _convertRgbToHex.convertRgbToHex)((0, _convertHslToRgb.convertHslToRgb)({
    hue: (hue + 180) % 360,
    saturation,
    lightness,
    alpha
  })), (0, _convertRgbToHex.convertRgbToHex)((0, _convertHslToRgb.convertHslToRgb)({
    hue: (hue + 270) % 360,
    saturation,
    lightness,
    alpha
  }))];
};
exports.getColorSchemeTetradic = getColorSchemeTetradic;
//# sourceMappingURL=getColorSchemeTetradic.js.map