"use strict";

var _blurFocusedElement = require("./blurFocusedElement");
var _getFocusedElement = require("./getFocusedElement");
describe("getFocusedElement", () => {
  test("Get focused element", () => {
    document.body.innerHTML = `<input id="input">`;
    const input = document.getElementById("input");
    input.focus();
    expect((0, _getFocusedElement.getFocusedElement)()).toBe(input);
    (0, _blurFocusedElement.blurFocusedElement)();
    expect((0, _getFocusedElement.getFocusedElement)()).not.toBe(input);
  });
});
//# sourceMappingURL=blurFocusedElement.test.js.map