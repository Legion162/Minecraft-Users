"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.isCprValid = void 0;
var _getCprRegex = require("../../regex/getCprRegex/getCprRegex");
var _convertToDate = require("../../convert/convertToDate/convertToDate");
/**
 * Check if a CPR number is valid.
 */
const isCprValid = (cprNumber, gender) => {
  const cpr = typeof cprNumber === "string" ? cprNumber : typeof cprNumber === "number" ? String(cprNumber) : cprNumber.value;

  // Check that the format is correct and that the date is valid
  if (!(0, _getCprRegex.getCprRegex)().test(cpr) || !(0, _convertToDate.convertToDate)(cpr.slice(0, 4) + "19" + cpr.slice(4, 6))) {
    return false;
  }

  // Should we check if the gender matches the CPR number?
  const checkGender = typeof gender === "string";

  // If not, return true
  if (!checkGender) {
    return true;
  }

  // Otherwise, check that the gender matches the CPR number
  const genderLetter = gender.charAt(0).toLowerCase();
  const lastDigit = parseInt(cpr.slice(-1));
  const lastDigitIsEven = lastDigit % 2;
  return !!(genderLetter === "f" && !lastDigitIsEven || genderLetter === "m" && lastDigitIsEven);
};
exports.isCprValid = isCprValid;
//# sourceMappingURL=isCprValid.js.map