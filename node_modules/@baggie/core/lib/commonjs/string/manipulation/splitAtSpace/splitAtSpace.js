"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.splitAtSpace = void 0;
const splitAtSpace = (string, nearestIndex) => {
  const trimmed = string.trim();
  const index = Math.max(nearestIndex, 1);
  const regex = new RegExp(`[\\S]{${index},}|([\\S].{0,${index - 1}}(?!\\S))`, "g");
  return trimmed.match(regex) || [trimmed];
};
exports.splitAtSpace = splitAtSpace;
//# sourceMappingURL=splitAtSpace.js.map