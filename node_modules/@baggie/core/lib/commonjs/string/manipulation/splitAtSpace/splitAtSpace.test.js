"use strict";

var _splitAtSpace = require("./splitAtSpace");
describe("splitStringAtSpace", () => {
  test("High index", () => {
    expect((0, _splitAtSpace.splitAtSpace)(`This text has a single email@address.com.`, 80)).toStrictEqual([`This text has a single email@address.com.`]);
  });
  test("Medium index", () => {
    expect((0, _splitAtSpace.splitAtSpace)(`This text has a single email@address.com.`, 10)).toStrictEqual(["This text", "has a", "single", "email@address.com."]);
  });
  test("Low index", () => {
    expect((0, _splitAtSpace.splitAtSpace)(`This text has a single email@address.com.`, 0)).toStrictEqual(["This", "text", "has", "a", "single", "email@address.com."]);
  });
});
//# sourceMappingURL=splitAtSpace.test.js.map