"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.intersperse = void 0;
/**
 * Insert new values between all existing values in an array.
 */
const intersperse = function (array) {
  for (var _len = arguments.length, insertion = new Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
    insertion[_key - 1] = arguments[_key];
  }
  return array.reduce((accumulator, element, index, array) => {
    accumulator.push(element);
    if (index < array.length - 1) {
      insertion.forEach((insert, insertIndex) => accumulator.push(typeof insert === "function" ? insert({
        previous: array[index],
        next: array[index + 1],
        index: insertIndex
      }) : insert));
    }
    return accumulator;
  }, []);
};
exports.intersperse = intersperse;
//# sourceMappingURL=intersperse.js.map